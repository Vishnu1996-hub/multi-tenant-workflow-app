# multi-tenant-workflow-app
Build a multi-tenant workflow and approval system that supports configurable workflows, approvals, concurrency safety, and auditability. The frontend is intentionally minimal and exists only to validate backend behavior.
